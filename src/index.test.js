import { BackButton, Pagination, LoadingBar, Select, Card, Counter } from '.'

describe('BackButton', () => {
  it('is truthy', () => {
    expect(BackButton).toBeTruthy()
  })
})

describe('Pagination', () => {
  it('is truthy', () => {
    expect(Pagination).toBeTruthy()
  })
})

describe('LoadingBar', () => {
  it('is truthy', () => {
    expect(LoadingBar).toBeTruthy()
  })
})

describe('Select', () => {
  it('is truthy', () => {
    expect(Select).toBeTruthy()
  })
})

describe('Card', () => {
  it('is truthy', () => {
    expect(Card).toBeTruthy()
  })
})

describe('Counter', () => {
  it('is truthy', () => {
    expect(Counter).toBeTruthy()
  })
})
